export interface APIUserTable {
	id: number;
	userName: string;
	department: string;
	privilege: string;
	userId: number;
	// insertPrivilege: boolean;
	// deletePrivilege: boolean;
	// updatePrivilege: boolean;
	// readPrivilege: boolean;
}
